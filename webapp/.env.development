# ============================================================
# SweetHandBraids — Development Overrides
# Routes chat + video room through local Vite API plugin
# instead of n8n cloud (which needs workflows imported first)
# ============================================================

# Use local Vite dev server for webhook endpoints
VITE_N8N_WEBHOOK_BASE=/webhook
